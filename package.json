{
  "name": "wordloop",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "description": "Offline vocabulary practice with easy English meanings",
  "scripts": {
    "build": "node scripts/build-data.mjs && node scripts/validate-data.mjs && node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/serve.mjs",
    "validate": "node scripts/validate-data.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
